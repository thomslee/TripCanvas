# 途迹 TripCanvas 后端环境变量配置
# 复制本文件为 .env 并填入真实值；.env 已在 .gitignore 中，不会提交到仓库

# 数据库
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=你的数据库密码
DB_NAME=trip_canvas

# Redis（预留，当前版本未使用）
REDIS_URL=redis://127.0.0.1:6379/0
